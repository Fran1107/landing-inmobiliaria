---
const testimonios = [
  {
    nombre: "María Elena Rodríguez",
    ubicacion: "Nordelta, Buenos Aires",
    avatar: "images/avatars/maria.webp",
    rating: 5,
    fecha: "Marzo 2024",
    testimonio: "Excelente atención desde el primer día. Me ayudaron a encontrar la casa de mis sueños en tiempo récord. El equipo de InmoPlus es muy profesional y siempre estuvieron disponibles para resolver mis dudas.",
    propiedad: "Casa 4 ambientes con jardín"
  },
  {
    nombre: "Carlos Mendoza",
    ubicacion: "Palermo, CABA",
    avatar: "images/avatars/carlos.webp",
    rating: 5,
    fecha: "Febrero 2024",
    testimonio: "Increíble experiencia comprando mi primer departamento. Me acompañaron en todo el proceso legal y financiero. Sin dudas los recomiendo para cualquier operación inmobiliaria.",
    propiedad: "Departamento 2 ambientes"
  },
  {
    nombre: "Ana Sofía García",
    ubicacion: "San Isidro, Buenos Aires",
    avatar: "images/avatars/ana.webp",
    rating: 5,
    fecha: "Enero 2024",
    testimonio: "Vendí mi casa en menos de un mes gracias a su estrategia de marketing. El precio obtenido superó mis expectativas y el proceso fue muy transparente en todo momento.",
    propiedad: "Casa familiar en barrio privado"
  },
  {
    nombre: "Roberto Silva",
    ubicacion: "Belgrano, CABA", 
    avatar: "images/avatars/roberto.webp",
    rating: 4,
    fecha: "Diciembre 2023",
    testimonio: "Muy buen servicio de alquiler. Me encontraron varias opciones que se ajustaban a mi presupuesto y me ayudaron con toda la documentación. Proceso rápido y eficiente.",
    propiedad: "Departamento 3 ambientes"
  },
  {
    nombre: "Laura Fernández",
    ubicacion: "Recoleta, CABA",
    avatar: "images/avatars/laura.webp",
    rating: 5,
    fecha: "Noviembre 2023",
    testimonio: "Profesionales de primer nivel. Me asesoraron perfectamente en la venta de mi departamento y la compra del nuevo. Todo se manejó con absoluta transparencia y confianza.",
    propiedad: "Penthouse en Recoleta"
  },
  {
    nombre: "Diego Morales",
    ubicacion: "Vicente López, Buenos Aires",
    avatar: "images/avatars/diego.webp",
    rating: 5,
    fecha: "Octubre 2023",
    testimonio: "Encontré mi casa ideal gracias a InmoPlus. El seguimiento post-venta también fue excelente, me ayudaron incluso con las conexiones de servicios. Muy recomendable.",
    propiedad: "Casa con pileta y quincho"
  }
];
---

<section id="testimonios" class="py-20 bg-white">
  <div class="container mx-auto px-4">
    <!-- Header -->
    <div class="text-center mb-16" data-animate>
      <div class="inline-flex items-center px-4 py-2 rounded-full bg-yellow-100 text-yellow-800 text-sm font-medium mb-4">
        ⭐ Testimonios
      </div>
      <h2 class="text-3xl md:text-5xl font-bold text-gray-900 mb-6">
        Lo que dicen nuestros clientes
      </h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        Miles de familias ya encontraron su hogar ideal con nosotros. 
        Lee sus experiencias reales.
      </p>
    </div>

    <!-- Testimonials Carousel Container -->
    <div class="relative" data-animate>
      <div class="overflow-hidden rounded-2xl">
        <div 
          id="testimonials-track"
          class="flex transition-transform duration-500 ease-in-out"
          style="transform: translateX(0%)"
        >
          {testimonios.map((testimonio, index) => (
            <div class="flex-shrink-0 w-full md:w-1/2 lg:w-1/3 px-4">
              <div class="bg-gray-50 rounded-xl p-8 h-full border border-gray-100 hover:shadow-lg transition-shadow duration-300">
                <!-- Rating -->
                <div class="flex items-center mb-4">
                  {Array.from({ length: 5 }, (_, i) => (
                    <svg 
                      class={`w-5 h-5 ${i < testimonio.rating ? 'text-yellow-400' : 'text-gray-300'}`} 
                      fill="currentColor" 
                      viewBox="0 0 20 20"
                    >
                      <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                    </svg>
                  ))}
                  <span class="ml-2 text-sm text-gray-600">{testimonio.fecha}</span>
                </div>

                <!-- Testimonio -->
                <blockquote class="text-gray-700 mb-6 leading-relaxed italic">
                  "{testimonio.testimonio}"
                </blockquote>

                <!-- Propiedad -->
                <div class="mb-6">
                  <div class="inline-flex items-center px-3 py-1 bg-blue-100 text-blue-800 text-xs rounded-full font-medium">
                    🏠 {testimonio.propiedad}
                  </div>
                </div>

                <!-- Cliente Info -->
                <div class="flex items-center">
                  <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-blue-600 rounded-full flex items-center justify-center text-white font-semibold mr-4">
                    {testimonio.avatar ? (
                      <img 
                        src={testimonio.avatar} 
                        alt={testimonio.nombre}
                        class="w-12 h-12 rounded-full object-cover"
                      />
                    ) : (
                      <span>{testimonio.nombre.split(' ').map(n => n[0]).join('')}</span>
                    )}
                  </div>
                  <div>
                    <div class="font-semibold text-gray-900">{testimonio.nombre}</div>
                    <div class="text-sm text-gray-600">{testimonio.ubicacion}</div>
                  </div>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Navigation Buttons -->
      <button 
        id="prev-btn"
        class="absolute left-0 top-1/2 -translate-y-1/2 -translate-x-4 w-12 h-12 bg-white border border-gray-300 rounded-full flex items-center justify-center text-gray-600 hover:text-blue-600 hover:border-blue-300 transition-colors shadow-lg hover:shadow-xl"
      >
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
        </svg>
      </button>

      <button 
        id="next-btn"
        class="absolute right-0 top-1/2 -translate-y-1/2 translate-x-4 w-12 h-12 bg-white border border-gray-300 rounded-full flex items-center justify-center text-gray-600 hover:text-blue-600 hover:border-blue-300 transition-colors shadow-lg hover:shadow-xl"
      >
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
        </svg>
      </button>

      <!-- Indicators -->
      <div class="flex justify-center mt-8 space-x-2" id="indicators">
        {testimonios.map((_, index) => (
          <button 
            class={`w-3 h-3 rounded-full transition-colors ${index === 0 ? 'bg-blue-600' : 'bg-gray-300'}`}
            data-index={index}
          />
        ))}
      </div>
    </div>

    <!-- CTA Section -->
    <section id="contacto" class="py-20 bg-white">
    <div class="text-center mt-16 bg-gradient-to-r from-blue-600 to-blue-800 rounded-3xl p-8 md:p-12 text-white" data-animate>
      <h3 class="text-2xl md:text-3xl font-bold mb-4">
        ¿Listo para ser nuestro próximo cliente satisfecho?
      </h3>
      <p class="text-blue-100 mb-8 max-w-2xl mx-auto">
        Únete a más de 1.200 familias que ya encontraron su hogar ideal con InmoPlus. 
        Tu próxima experiencia exitosa nos espera.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a 
          href="/propiedades"
          class="inline-flex items-center justify-center px-8 py-4 bg-white text-blue-600 rounded-xl font-semibold text-lg hover:bg-blue-50 transition-colors"
        >
          Ver propiedades
          <svg class="ml-2 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
          </svg>
        </a>
        <a 
          href="/contacto"
          class="inline-flex items-center justify-center px-8 py-4 border-2 border-white/30 hover:border-white/50 text-white rounded-xl font-semibold text-lg transition-colors"
        >
          Contactanos
          <svg class="ml-2 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
          </svg>
        </a>
      </div>
    </div>
  </section>
  </div>
</section>

<script define:vars={{ testimonios }}>
  document.addEventListener('DOMContentLoaded', () => {
    const track = document.getElementById('testimonials-track');
    const prevBtn = document.getElementById('prev-btn');
    const nextBtn = document.getElementById('next-btn');
    const indicators = document.querySelectorAll('#indicators button');
    
    let currentIndex = 0;
    const totalTestimonios = testimonios.length;
    const testimonialsPerView = window.innerWidth >= 1024 ? 3 : window.innerWidth >= 768 ? 2 : 1;
    const maxIndex = Math.max(0, totalTestimonios - testimonialsPerView);

    function updateCarousel() {
      const translateX = -(currentIndex * (100 / testimonialsPerView));
      track.style.transform = `translateX(${translateX}%)`;
      
      // Update indicators
      indicators.forEach((indicator, index) => {
        indicator.classList.toggle('bg-blue-600', index === currentIndex);
        indicator.classList.toggle('bg-gray-300', index !== currentIndex);
      });
    }

    function nextSlide() {
      currentIndex = currentIndex >= maxIndex ? 0 : currentIndex + 1;
      updateCarousel();
    }

    function prevSlide() {
      currentIndex = currentIndex <= 0 ? maxIndex : currentIndex - 1;
      updateCarousel();
    }

    // Event listeners
    nextBtn?.addEventListener('click', nextSlide);
    prevBtn?.addEventListener('click', prevSlide);

    indicators.forEach((indicator, index) => {
      indicator.addEventListener('click', () => {
        currentIndex = Math.min(index, maxIndex);
        updateCarousel();
      });
    });

    // Auto-advance carousel
    setInterval(() => {
      nextSlide();
    }, 5000);

    // Handle window resize
    window.addEventListener('resize', () => {
      const newTestimonialsPerView = window.innerWidth >= 1024 ? 3 : window.innerWidth >= 768 ? 2 : 1;
      currentIndex = Math.min(currentIndex, testimonios.length - newTestimonialsPerView);
      updateCarousel();
    });
  });
</script>